name: Build and Deploy
on: [push] # The pipeline runs every time you push code

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build Docker Image
        run: |
          docker build -t my-app .
          echo "Build successful!"

      - name: Deploy to Production
        env:
          MY_KEY: ${{ secrets.PROD_API_KEY }} # The pipeline loads the secret here
        run: |
          echo "Deploying to server using the secret..."
          # Imagine a real deployment command here
